let theme = () => {
    let themes = ['dark', 'light', 'navy'];
    let theme = themes[i];
    let selector = () => {
        if (theme = clicked) {
            let [i] = clicked;
            theme = themes[i];
        }
    }
    return theme;
}
